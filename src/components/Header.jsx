const Header = () => {
  return (
    <div style={styles.header}>
      EduMate AI – Personalized Learning Assistant 
    </div>
  );
};

const styles = {
  header: {
    padding: "15px",
    background: "#1e293b",
    color: "white",
    textAlign: "center",
    fontSize: "20px",
    fontWeight: "bold"
  }
};

export default Header;
